# clawket-ssot

Clawket(LLM-네이티브 작업관리 + 로컬 RAG) 제품의 SSOT — 제품·도메인·시스템 맥락을 노드 그래프로 외부화.
플러그인·Rust CLI/데몬·웹/데스크탑 코드에서 역추론한 자연어 정책/동작/데이터 문서. confidence 대부분 inferred(담당자 검증 대기).

- 데이터: `ssot/` (노드 .md + `_catalog.json`)
- 뷰어: push 시 ssot-studio/ssot-web 빌드+주입해 gh-pages 배포(가능 시).
